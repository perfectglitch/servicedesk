plugins {
    id 'com.moowork.node' version '1.3.1'
}

node {
    download = true
}

task bundle(type: NpmTask, dependsOn: npmInstall) {
    args = ['run', 'build']
}

task npmTest(type: NpmTask) {
    environment = ['CI': 'true']
    args = ['run', 'test']
}

bundle.dependsOn npmTest
